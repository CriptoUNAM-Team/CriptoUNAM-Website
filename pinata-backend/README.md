# pinata-backend (legado)

API Express usada cuando el backend vivía fuera de Vercel. El sitio actual persiste en **Supabase** y las rutas nuevas están en `criptounam/api/`.

No hace falta levantarlo para desarrollar el front. Se conserva por referencia (auth de wallets, modelos de cursos/eventos).
